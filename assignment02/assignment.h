#include <stdio.h>

#include "linked_list.h"

/*
    Given a map, return distance to the closest water source from the camp.
    Campers are only allowed to move 4 directions.
    'C' = the camp (only one camp)
    '.' = empty land
    'W' = water source
    '#' = obstacle

    if no reachable water source return -1
*/
int closestWaterDistance(char* fileName);

/*
    String can be collapsed by erasing 3 adjecent character that are all same.
    Given a string, return a fully collapsed string (collapse repeatedly until 
    no longer possible).

    Solve this in O(n) time and space.
*/
char* fullyCollapseString(char* word);



int closestWaterDistance(char* fileName) {
}

char* fullyCollapseString(char* word) {
}